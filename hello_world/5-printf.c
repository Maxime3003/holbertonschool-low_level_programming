#include <stdio.h>

/**
 * main - entry point 
 * Description: inclure un texte dans le programme avec la commande printf
 * Return: Always 0 (Success)
 */

int main(void)

{
	printf(\"with proper grammar, but the outcome is a piece of art,");
	return (0);
}
